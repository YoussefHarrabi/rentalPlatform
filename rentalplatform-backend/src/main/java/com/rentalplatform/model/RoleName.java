package com.rentalplatform.model;

public enum RoleName {
    ROLE_ADMIN,      // Platform administrator
    ROLE_OWNER,      // Equipment owner (can create listings)
    ROLE_CLIENT      // Client (can make reservations)
}